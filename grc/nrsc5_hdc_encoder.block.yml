# auto-generated by grc.converter

id: nrsc5_hdc_encoder
label: HDC Encoder
category: '[NRSC-5]'

parameters:
-   id: channels
    label: Channels
    dtype: int
    default: 2
-   id: bitrate
    label: Bitrate
    dtype: int
    default: 64000
-   id: ps_enable
    label: Parametric Stereo
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['Disabled', 'Enabled']

inputs:
-   domain: stream
    dtype: float
    multiplicity: ${ channels }

outputs:
-   domain: stream
    dtype: byte
asserts:
- ${ 1 <= channels <= 2 }
- ${ 0 < bitrate }
- ${ (not ps_enable) or (channels == 2) }

templates:
    imports: import nrsc5
    make: nrsc5.hdc_encoder(${channels}, ${bitrate}, ${ps_enable})

file_format: 1
